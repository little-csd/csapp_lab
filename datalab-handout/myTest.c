#include<stdio.h>
int howManyBits(int x)
{
  int mask = (x&(1<<31))>>31;
  x = x^mask;
  int sum = 1;
  sum += (x&1) + ((x>>1)&1) + ((x>>2)&1) + ((x>>3)&1);
  x>>=4;
  sum += (x&1) + ((x>>1)&1) + ((x>>2)&1) + ((x>>3)&1);
  x>>=4;
  sum += (x&1) + ((x>>1)&1) + ((x>>2)&1) + ((x>>3)&1);
  x>>=4;
  sum += (x&1) + ((x>>1)&1) + ((x>>2)&1) + ((x>>3)&1);
  x>>=4;
  sum += (x&1) + ((x>>1)&1) + ((x>>2)&1) + ((x>>3)&1);
  x>>=4;
  sum += (x&1) + ((x>>1)&1) + ((x>>2)&1) + ((x>>3)&1);
  x>>=4;
  sum += (x&1) + ((x>>1)&1) + ((x>>2)&1) + ((x>>3)&1);
  x>>=4;
  sum += (x&1) + ((x>>1)&1) + ((x>>2)&1) + ((x>>3)&1);
  x>>=4;
  return sum;
}
int main()
{
  int n;
  while(scanf("%d", &n)) {
	  printf("%d\n", howManyBits(n));
  }
}
